# Normalize all text files to LF in the repo and on checkout,
# overriding any local core.autocrlf=true (which was flipping every
# touched file to CRLF on Windows).
* text=auto eol=lf

# Windows-native scripts need CRLF to run correctly everywhere
*.bat text eol=crlf
*.ps1 text eol=crlf
